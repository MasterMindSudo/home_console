# Todo and Voice Module Placeholder

The transit dashboard ships first. The database already includes a `todo_tasks`
table so a later module can add typed reminders and OpenAI-powered voice intent
parsing without reshaping the app.
